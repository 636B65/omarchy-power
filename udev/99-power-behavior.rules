# Apply power behavior (AC: never sleep / Battery: sleep) when the AC
# adapter is plugged in or unplugged. Runs as root via udev.
ACTION=="change", SUBSYSTEM=="power_supply", ATTR{type}=="Mains", RUN+="/home/user/.config/omarchy/power/switch-power-behavior.sh"
